using System;
using Godot;

enum Controls
{
    Left,
    Right,
    Throw1,
    Throw2
}

enum CustomDataNames
{
    tilePosition
}

enum PositionOnTile
{
    ON_LEFT_EDGE = -1,
    ON_RIGHT_EDGE = 1
}

